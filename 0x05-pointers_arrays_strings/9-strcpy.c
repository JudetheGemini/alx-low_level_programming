#include "main.h"
#include <string.h>
/**
 **_strcpy - copy string to buffer
 *@dest: destination
 *@src: string source
 * Return: string
 */
char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
	return (dest);
}
